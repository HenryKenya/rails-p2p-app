
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-content" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Zeecash | alpha', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-right" id="navbar-collapse-content">
      <ul class="nav navbar-nav">
         <% if user_signed_in? %>
            <li class="navbar-with-border-right"><a href="#!">Ksh. <%= current_user.balance %></a></li>
            <li class=""><%= link_to 'New', new_transaction_path %></li>
            <li class="dropdown pc-menu navbar-with-border">
              <a id="user-settings" class="dropdown-toggle" data-toggle="dropdown" href="#">
                <span id="user-name"><%= current_user.email %></span>
                <span class="caret"></span>
              </a>

              <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
              <li><a href="#!">Transaction records</a></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>

            </ul>

            </li>

            

      <% else # user not signed it %>
          <li class="navbar-with-border"><%= link_to 'Login', login_path %></li>
          <li class="navbar-with-border"><%= link_to 'Signup', signup_path %></li>
      <% end # if user is signed it %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

